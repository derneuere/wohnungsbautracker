node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Raw scraped HTML
data/raw/
data/raw-*/

# Datenbank wird versioniert (dient als Deploy-Artefakt, siehe Dockerfile)

# Lokale Sicherungen der Datenbank vor Recherche-Durchgaengen
sqlite.db.backup-*

# Projekt baut mit Bun (bun.lock); npm-Lockfile waere widerspruechlich
package-lock.json
